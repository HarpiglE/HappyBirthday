<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harpigle.happybirthday">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenuActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Persons.Activity.AddPersonActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Persons.Activity.PersonsListActivity"
            android:screenOrientation="portrait" />

        <!--
             if you are using ActionBarActivity (or AppCompat theme),
             you'll need to use @style/Theme.AppCompat.Dialog.
             else you can use @android:style/Theme.Holo.Dialog
        -->
        <activity
            android:name=".Persons.PersonsListActivityEditionDialog"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert" />
        <activity
            android:name=".Messages.Activity.AddMessageActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>